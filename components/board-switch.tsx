'use client';
export const BOARD_REGISTRY=[{id:'workflow',label:'工作流',href:'/'},{id:'time',label:'时间',href:'/time'}] as const;
export function BoardSwitch({current,space='personal'}:{current:'workflow'|'time';space?:string}){return <select aria-label="切换看板" value={current} onChange={e=>{const board=BOARD_REGISTRY.find(x=>x.id===e.target.value);if(board){localStorage.setItem('qs-selected-space',space);location.href=board.href;}}} style={{font:'inherit',fontSize:12,marginLeft:10,padding:'2px 5px',border:'1px solid currentColor',borderRadius:3,background:'transparent',color:'inherit',maxWidth:90,verticalAlign:'middle'}}>{BOARD_REGISTRY.map(board=><option key={board.id} value={board.id}>{board.label}</option>)}</select>;}
