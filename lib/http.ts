import {AppError} from './workspace';
import {ConflictError,type Space} from './protocol';
export function spaceOf(v:unknown):Space{if(v!=='personal'&&v!=='demo')throw new AppError('工作区无效');return v;}
export function json(data:unknown,status=200){return Response.json(data,{status,headers:{'Cache-Control':'no-store','X-Content-Type-Options':'nosniff'}});}
export function failure(e:unknown){if(e instanceof ConflictError)return json({error:e.message,code:e.code,latest:e.latest},409);if(e instanceof AppError)return json({error:e.message,code:e.status===410?'SYNC_RESET_REQUIRED':undefined},e.status);console.error(JSON.stringify({code:'REQUEST_FAILED',name:e instanceof Error?e.name:'unknown'}));return json({error:'服务暂时不可用，操作已保留，请重试'},500);}
export async function body(request:Request){const origin=request.headers.get('origin');if(origin&&origin!==new URL(request.url).origin)throw new AppError('请求来源不匹配',403);if(request.headers.get('sec-fetch-site')==='cross-site')throw new AppError('请求来源不匹配',403);if(!request.headers.get('content-type')?.includes('application/json'))throw new AppError('需要 JSON 请求');const reader=request.body?.getReader();if(!reader)throw new AppError('缺少请求内容');let size=0;const chunks:Uint8Array[]=[];while(true){const r=await reader.read();if(r.done)break;size+=r.value.byteLength;if(size>4_000_000){await reader.cancel();throw new AppError('请求不能超过 4 MB',413);}chunks.push(r.value);}const all=new Uint8Array(size);let at=0;for(const c of chunks){all.set(c,at);at+=c.length;}try{return JSON.parse(new TextDecoder().decode(all));}catch{throw new AppError('JSON 格式无效');}}
